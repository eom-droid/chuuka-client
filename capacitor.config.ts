import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.web.chuuka.client',
  appName: 'Chuuka',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
